#include <bits/stdc++.h>
using namespace std;
using ll=long long;
const ll N=1e6+10;
ll mod = 

int main(){
    vector<ll> fact(1000000);
    ll a,b; cin>>a>>b; vector<ll>m(a), n(b);
    for (auto& i:m) cin>>i; 
    for (auto& i:n) cin>>i;
    vector<vector<ll>>coef(N,vector<ll>(N));
    coef[0][0]=1;
    for (ll i=0, i<)



}